# 这个实验怎么做（大白话版）

这份文件只讲两件事：**每一步具体敲什么命令**，以及**出错时怎么办**。

---

## 一、先搞清楚你电脑上有什么

| 东西 | 在哪 | 说明 |
| --- | --- | --- |
| conda 环境 py | `E:\conda_envs\py` | Python 3.11.16 + PyTorch + numpy/pandas/matplotlib，实验就用它 |
| Git | `E:\tools\MinGit\cmd\git.exe` | 便携版 Git 2.55，已加进用户 PATH |
| 项目代码 | `C:\Users\贺斌杰\Documents\ChatGPT\stage_one` | 就是你要交的东西 |
| PyCharm | `E:\pycharm\PyCharm 2026.2.2` | 缓存已挪到 `E:\pycharm-system`，不占 C 盘 |
| 仓库地址 | https://github.com/h2719013513-gif/stage_one | 老师要的仓库链接 |
| 双击就能跑的脚本 | 项目里的 `run_exp1_env_check.bat`、`run_exp2_data_analysis.bat` | 不用敲任何命令 |

**注意**：PyTorch、conda 环境、Git、PyCharm 缓存全部放在 E 盘，C 盘只剩 9.8GB，以后装东西也尽量装 E 盘。

---

## 二、在你这台电脑上具体怎么执行（三种方式，挑一种就行）

三种方式跑出来结果完全一样，都是同一个解释器 `E:\conda_envs\py\python.exe`。

### 方式 1：双击 bat 脚本（最省事，推荐平时自己跑）

打开项目文件夹 `C:\Users\贺斌杰\Documents\ChatGPT\stage_one`，里面有两个文件：

| 双击这个文件 | 会发生什么 |
| --- | --- |
| `run_exp1_env_check.bat` | 跑实验一的环境检测 |
| `run_exp2_data_analysis.bat` | 跑实验二的数据处理，生成 CSV 和 4 张图 |

双击后会弹出一个黑窗口，它自己会用 `E:\conda_envs\py\python.exe` 去跑，
跑完停在「按任意键关闭本窗口」。**不用激活 conda，也不用敲任何命令。**

### 方式 2：命令行（验收当天用这个，老师看得最清楚）

1. 按 `Win + R`，输入 `cmd`，回车；
2. 一行一行敲，每行回车：

```bat
conda activate py
cd /d C:\Users\贺斌杰\Documents\ChatGPT\stage_one
python check_env.py
python data_analysis.py
```

两个备选写法，遇到问题就用：

- 如果提示 `conda 不是内部或外部命令`（conda 没初始化），改成：

```bat
E:\anaconda3\Scripts\activate.bat py
```

- 如果连环境都懒得激活，直接用全路径（bat 脚本里干的就是这个）：

```bat
E:\conda_envs\py\python.exe check_env.py
E:\conda_envs\py\python.exe data_analysis.py
```

**怎么判断跑成功：**

- `check_env.py`：最后一行是 `检测结论：环境检测完成，可以开始深度学习实验。`
- `data_analysis.py`：最后一行是 `实验 2 运行完成。`，并且 `output\figures` 下多了 4 张 PNG。

**想把运行结果存成文本交给老师：**

```bat
E:\conda_envs\py\python.exe check_env.py > output\check_env_output.txt 2>&1
E:\conda_envs\py\python.exe data_analysis.py > output\data_analysis_output.txt 2>&1
```

### 方式 3：在 PyCharm 里点绿色三角

1. 打开 PyCharm（任务栏图标，或直接运行 `E:\pycharm\PyCharm 2026.2.2\bin\pycharm64.exe`），项目 `stage_one` 已经打开；
2. 左边文件树里双击 `check_env.py`；
3. 按 `Ctrl + Shift + F10`，或者点右上角绿色 ▶；
4. 下方 `Run` 窗口就是运行结果；
5. 对 `data_analysis.py` 重复同样操作；
6. 跑完在左边 `output/figures` 目录里双击 PNG 就能直接看图；
7. 右下角应显示解释器 `Python 3.11 (py)`。如果不对，点它选一下；列表里没有就看下面「问题 11」。

### 三种方式跑完都会产生这些文件

| 文件 | 每次运行会怎样 |
| --- | --- |
| `data\processed\students_scores_clean.csv` | 覆盖重写（数据没变就不会变） |
| `output\statistics_summary.csv` | 覆盖重写 |
| `output\figures\fig1~fig4*.png` | 覆盖重写（图里的随机性来自数据本身，不用重跑脚本） |

---

## 三、实验一：环境检测（check_env.py）

### 步骤

1. 按 `Win + R`，输入 `cmd`，回车（开一个命令行窗口）。
2. 依次敲这三行，每行回车：

```bat
conda activate py
cd /d C:\Users\贺斌杰\Documents\ChatGPT\stage_one
python check_env.py
```

### 成功的样子

屏幕上会看到：

```
Python 版本   : 3.11.16
PyTorch 版本  : 2.14.0+cu126
编译所用 CUDA : 12.6
是否支持 CUDA : 是
  显卡 0       : NVIDIA GeForce RTX 3050 Laptop GPU（显存 4.0 GB）

tensor([[0.4168, 0.9716, 0.0901],
        [0.7068, 0.3501, 0.5201],
        [0.7271, 0.4372, 0.3346]])
```

最后一行会打印 `检测结论：环境检测完成`。数字每次不一样（张量是随机的），这是正常的。

### 老师会问：CPU 和 GPU 有什么区别？

照着这三句说就行：

1. CPU 核心少（几个到十几个），但每个核什么都能干，适合逻辑判断、循环、调试；
2. GPU 核心多（上千个），但每个核只会做简单运算，适合大量矩阵乘法；
3. 深度学习训练主要就是矩阵乘法，所以 GPU 能快几十倍。

补一句加分：`torch.rand(3,3)` 默认建在 **CPU** 上，要写 `.to('cuda')` 才会搬到显卡，程序里已经做了这一步。

---

## 四、实验二：数据处理与可视化（data_analysis.py）

### 步骤

在同一个命令行窗口里敲：

```bat
python data_analysis.py
```

### 程序干了 5 件事

1. 读 `data/raw/students_scores_raw.csv`（原始数据，122 条，里面故意有脏数据）；
2. 清洗：删掉重复学号、把空白和"缺考""无"当成缺失、把 `-1`/`200`/`999`/出勤率大于 1 当成异常剔除，再用**同班级同科目的中位数**补齐；
3. 算平均值、最大值、最小值、标准差、中位数；
4. 画 4 张图；
5. 把处理后的数据另存为新 CSV。

### 结果在哪

| 文件 | 是什么 |
| --- | --- |
| `data\processed\students_scores_clean.csv` | 清洗后的数据（121 条，多了"平均分""等级"两列） |
| `output\statistics_summary.csv` | 平均值、最大值、最小值、标准差的汇总表 |
| `output\figures\fig1_成绩分布直方图.png` | 三门课成绩分布 |
| `output\figures\fig2_班级平均分柱状图.png` | 各班各科平均分对比 |
| `output\figures\fig3_成绩箱线图.png` | 成绩离散程度 |
| `output\figures\fig4_等级分布饼图.png` | 优秀/良好/中等/及格人数占比 |

### 老师会问：缺失值和异常值怎么处理的？

照着答：

> 先分区处理再补。空白、"缺考"、"无"这些是缺失值；`-1`、`200`、`999`、出勤率大于 1 是异常值，因为超出合理范围。
> 两种都清成空，然后用**同一个班级、同一门课的中位数**填回去，这样不会被人为拉高或拉低。
> 重复的学生记录按学号只保留第一条。全流程都在函数里，不靠手工改 CSV。

---

## 五、实验三：Git（提交代码到仓库）

### 先确认 Git 能用

**新开**一个 cmd（老窗口是改 PATH 之前开的，认不出 git），敲：

```bat
git --version
```

显示 `git version 2.55.0.windows.5` 就对了。如果提示"不是内部或外部命令"，用全路径：

```bat
E:\tools\MinGit\cmd\git.exe --version
```

### 最常用的 5 条命令

先 `cd /d` 进到项目目录，然后：

```bat
git status                     :: 看有哪些改动
git add .                      :: 把所有改动加入本次提交
git commit -m "这次改了啥"      :: 提交（引号里写清楚改了什么）
git log --oneline              :: 看提交记录
git push                       :: 推到 GitHub
```

### 已经做好的事

- 仓库已经初始化，并且关联了 `origin` = https://github.com/h2719013513-gif/stage_one
- 已经推上去 **10 次**提交（老师只要求至少 3 次）
- 提交人显示为 `贺斌杰 <h2719013513-gif@users.noreply.github.com>`，会正确关联到你的 GitHub 账号

---

## 六、可能出现的问题（按现象查）

### 1. 敲 python 提示"不是内部或外部命令"

原因：没激活 conda 环境。
解决：先敲 `conda activate py`。如果连 `conda` 都不认识，用全路径：

```bat
E:\anaconda3\Scripts\activate.bat py
```

### 2. 建环境时报"找不到 python"或"找不到 pip"

原因：这台机器的 conda 默认求解器（libmamba）取索引有问题，会误报包不存在。
解决：创建环境时加 `--solver=classic`，并指定清华镜像：

```bat
conda create -p E:\conda_envs\py python=3.11 pip -y --solver=classic --override-channels -c https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/
```

### 3. 装 PyTorch 特别慢（1 MB/s 都不到）

原因：官方源 `download.pytorch.org` 在国内慢。
解决：从上海交大镜像下载 wheel 再本地安装（实测 6~11 MB/s）：

```bat
curl.exe -L -o E:\torch_dl\torch.whl "https://mirror.sjtu.edu.cn/pytorch-wheels/cu126/torch-2.14.0%2Bcu126-cp311-cp311-win_amd64.whl"
pip install --no-cache-dir E:\torch_dl\torch.whl
```

### 4. `torch.cuda.is_available()` 是 False

三个原因，按顺序查：

1. 装的是 CPU 版 PyTorch（`pip install torch` 默认下载的就是 CPU 版）；
2. 显卡驱动太旧（这台机器驱动 591.74，没问题）；
3. 显卡不是 NVIDIA（这台是 RTX 3050，没问题）。

解决：重装 CUDA 版 `pip install torch --index-url https://download.pytorch.org/whl/cu126`。

### 5. 图片里的中文变成一个个方框

原因：matplotlib 没找到中文字体。
解决：代码里已经设了 `plt.rcParams["font.sans-serif"] = ["Microsoft YaHei", "SimHei", "SimSun"]`。
如果换到别的电脑还出现方框，把这一行改成那台电脑有的中文字体名即可。

### 6. 报错 `ModuleNotFoundError: No module named 'pandas'`

原因：用错 Python 了（没激活环境，用的是系统的 E:\python）。
解决：`conda activate py`，再运行。确认用的是哪个 Python 可以敲 `python -c "import sys; print(sys.executable)"`，应该显示 `E:\conda_envs\py\python.exe`。

### 7. CSV 用 Excel 打开是乱码

原因：编码不对。
解决：本项目的 CSV 都保存成 `utf-8-sig`，直接用 Excel 双击就能正常显示。如果你自己写代码存 CSV，记得加 `encoding="utf-8-sig"`。

### 8. `git` 命令找不到

原因：老终端窗口是在改 PATH 之前打开的。
解决：把那个窗口关掉，重新开一个。或者直接用全路径 `E:\tools\MinGit\cmd\git.exe`。

### 9. `git push` 要用户名和密码

原因：GitHub 从 2021 年起不再支持用登录密码推送。
解决：用 **Personal Access Token**（个人访问令牌）当密码。生成位置：GitHub → 右上角头像 → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token，勾选 `repo`，复制出来的那串就是密码。
用户名填你的 GitHub 用户名，密码粘贴 token。

### 10. `git` 报 `dubious ownership`（仓库属主可疑）

原因：用不同 Windows 账号执行了 git。
解决：

```bat
git config --global --add safe.directory C:/Users/贺斌杰/Documents/ChatGPT/stage_one
```

### 11. PyCharm 里没有解释器 / 代码全是红波浪线

原因：项目解释器没选中。
解决：右下角点解释器 → 选 `Python 3.11 (py)`。
如果列表里没有：`Ctrl+Alt+S` → `Project: stage_one` → `Python Interpreter` → `Add Interpreter` → `Add Local Interpreter` → `Conda Environment` → `Existing environment` → 路径填 `E:\conda_envs\py\python.exe`。
（本机已经帮你配好了，正常情况下直接就是对的。）

### 12. PyCharm 启动报 `Start Failed` 或 `DirectoryLock`

原因：上一次 PyCharm 卡死，进程还在占着锁。
解决：

1. `Ctrl + Shift + Esc` 打开任务管理器，结束所有 `pycharm64.exe`；
2. 删掉 `C:\Users\贺斌杰\AppData\Roaming\JetBrains\PyCharm2026.2\.lock`；
3. 重新启动 PyCharm。

### 13. C 盘又满了

解决：目前大头都在 E 盘（conda 环境、torch wheel、PyCharm 缓存）。可以顺手删掉下载缓存：

```bat
rmdir /s /q E:\torch_dl
```

（这 2.4GB 是 PyTorch 安装包，删了不影响已装好的环境，只是以后重装要重新下载。）

---

## 七、验收当场怎么演示（4 步）

1. 开 cmd，敲 `conda activate py`；
2. 敲 `python check_env.py`，指着 CUDA = 是、显卡是 RTX 3050、3×3 张量说一遍；
3. 敲 `python data_analysis.py`，说清洗规则（缺失/异常/中位数填补），打开 `output\figures` 里的图；
4. 打开浏览器进 https://github.com/h2719013513-gif/stage_one ，点 `commits` 让老师看提交记录（10 次）。
